# frozen_string_literal: true

RSpec.describe Codebreaker21rg do
  it "has a version number" do
    expect(Codebreaker21rg::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
